import { PhotoArchive } from '@/components/photo-archive';
export const dynamic = 'force-static';
export const metadata = { title: 'Photography — Personal Archive', description: 'Between Windows / Greenland Summer School / Everyday Specimens' };
export default function PhotographyPage() { return <PhotoArchive />; }
